# AutoCV — stack local (licencias permisivas: MIT, PSF, BSD)
# SQLAlchemy 2.x — ORM + SQLite
sqlalchemy>=2.0.36,<3.0
# Automatización de navegador
playwright>=1.60.0,<2.0
# Coincidencia difusa de texto (MIT)
rapidfuzz>=3.10.0,<4.0
# Validación y modelos de datos (MIT)
pydantic>=2.10.0,<3.0
# Cliente HTTP async/sync (BSD)
httpx>=0.28.0,<1.0
# Variables de entorno desde .env (BSD)
python-dotenv>=1.0.0,<2.0
# Scraping HTTP + parseo HTML (PSF / MIT)
requests>=2.32.0,<3.0
beautifulsoup4>=4.12.0,<5.0
